---
const base = import.meta.env.BASE_URL || '/';
---

<header class="site-header">
  <div class="header-inner container-wide">
    <a href={base} class="site-brand">
      <img
        src={`${base}brand/logo-mark-light.png`}
        alt="LUCENTIQUE"
        class="brand-logo"
      />
      <span class="brand-copy">
        <span class="brand-text">
          <span class="brand-name">LUCENTIQUE</span><span class="brand-cn">古籍藏</span>
        </span>
        <span class="brand-tagline">MEET YOUR MOON</span>
      </span>
    </a>
    <nav class="site-nav" aria-label="主导航">
      <a href={`${base}a/`}>藏品</a>
      <span class="nav-sep">·</span>
      <a href={`${base}b/`}>老灵魂</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    padding: 1.6rem 0;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .header-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .site-brand {
    display: inline-flex;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    gap: 0.72rem;
    border-bottom: none;
    text-decoration: none;
    line-height: 1;
    min-width: 0;
  }

  .brand-logo {
    height: 38px;
    width: auto;
    display: block;
    flex-shrink: 0;
  }

  .brand-copy {
    display: flex;
    flex-direction: column;
    gap: 0.32rem;
    min-width: 0;
  }

  .brand-text {
    display: inline-flex;
    align-items: baseline;
    font-family: var(--font-serif);
    font-size: 1rem;
    letter-spacing: 0;
    color: var(--color-text);
    line-height: 1;
    white-space: nowrap;
  }

  .brand-name {
    letter-spacing: 0.16em;
  }

  .brand-cn {
    font-size: 0.9em;
    letter-spacing: 0.04em;
    margin-left: 0.12rem;
  }

  .brand-tagline {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.42rem;
    letter-spacing: 0.48em;
    color: #b69b5c;
    line-height: 1;
    white-space: nowrap;
  }

  .site-nav {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    flex-shrink: 0;
  }

  @media (max-width: 520px) {
    .header-inner {
      align-items: center;
      flex-direction: row;
      gap: var(--space-sm);
    }

    .site-nav {
      width: 100%;
      justify-content: center;
    }

    .brand-text {
      font-size: 0.9rem;
    }

    .brand-tagline {
      font-size: 0.38rem;
      letter-spacing: 0.36em;
    }
  }

  .site-nav a {
    border-bottom: none;
    color: var(--color-text-muted);
  }

  .site-nav a:hover {
    color: var(--color-text);
    border-bottom: none;
  }

  .nav-sep {
    color: var(--color-border);
  }
</style>
